```text

horse[2] = 0
horse[3

horses = [0, 0, 0, 0, 0] 

advance(int horseNum, int* horses)
  horseNum  will be passed into this function
  create a random value representing heads or tails [ 1(heads)  and 0(tails) ]
  if coin = 1
    
  else
    horse does not move at all

  
void printLane(int horseNum, int* horses);


bool isWinner(int horseNum, int* horses);

